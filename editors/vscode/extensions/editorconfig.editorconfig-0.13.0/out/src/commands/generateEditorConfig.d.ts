import { Uri } from 'vscode';
/**
 * Generate a .editorconfig file in the root of the workspace based on the
 * current vscode settings.
 */
export declare function generateEditorConfig(uri: Uri): void;
